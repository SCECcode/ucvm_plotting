### this are steps to build the dist of ucvm_plotting
### to install ==>>>   pip install dist/ucvm_plotting-0.0.1.tar.gz

rm -rf dist
mkdir dist

#pip install pep517
#mkdir dist
python -m pep517.build .
