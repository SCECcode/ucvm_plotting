name: ucvm_plotting-ci


on: [push]

jobs:
  ucvm_plotting-build:
    runs-on: ubuntu-latest
    name: A job to build ucvm_plotting
    steps:
    - uses: actions/checkout@v2
    - run: ( echo "UCVM_PLOTTING_PATH=$RUNNER_WORKSPACE/ucvm_plotting" >> $GITHUB_ENV )
      shell: bash
#    - id: build-ucvm
#      uses: SCECcode/UCVMC/.github/actions@action1
#      with:
#        target-models: 'cvmh'
#    - run: echo build-log ${{ steps.build-ucvm.outputs.build-log }}
#      shell: bash
    - name: Set up Python 2.7.17
      uses: actions/setup-python@v2
      with:
        python-version: '2.7.17'
        architecture: 'x64'
    - name: Display Python version
      run: python -c "import sys; print(sys.version)"
    - name: Install dependencies
      run: |
        $CONDA/bin/conda env update --file environment.yml --name base python=2.7.17
#        pip install -c conda-forge  matplotlib basemap basemap-data-hires
#        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        conda list
