### this are steps to build the dist of ucvm_plotting
### to install ==>>>   pip install dist/ucvm_plotting-0.0.6.tar.gz

#rm -rf dist build
#mkdir dist
#
# using build from https://pypi.org/project/build/
#
# base on configuration files : setup.cfg and setup.py

pip install build
python -m build .
